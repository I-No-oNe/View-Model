# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx4G -Xms1G -XX:+UseParallelGC -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true
org.gradle.workers.max=3

# Fabric Properties
# check these on https://fabricmc.net/develop/
minecraft_version=26.1
loader_version=0.19.2
loom_version=1.16-SNAPSHOT

# Fabric API
fabric_api_version=0.145.1+26.1

# check this via:
# read -p "echo 'MC Version: '" v; curl -s "https://api.modrinth.com/v2/project/midnightlib/version" | jq -r --arg v "$v" '.[] | select(.loaders[]=="fabric" and (.game_versions|contains([$v]))) | .version_number' | head -n 1
midnightlib_version=1.9.2+26.1-fabric

# Mod Properties
mod_version=1.0.4-26.1-fabric
maven_group=net.i_no_am.view_model
archives_base_name=view-model
